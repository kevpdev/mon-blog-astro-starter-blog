---
description: Common workflow patterns and reusable components
alwaysApply: false
---

# Workflow Patterns

## Standard Validation Commands

```bash
# Quality validation sequence (used in all workflows)
pnpm quality              # TypeScript + Lint + Format check
pnpm typecheck           # TypeScript validation  
pnpm build               # Production build test
pnpm test                # Run test suite
```

## Project Health Check

```bash
# Environment validation
echo "Node: $(node --version)"
echo "PNPM: $(pnpm --version)"
echo "Branch: $(git branch --show-current)"

# Project status
git status --porcelain | wc -l
pnpm outdated | head -5
```

## Common Checklists

### Quality Checklist Template
```markdown
- [ ] TypeScript compilation passes
- [ ] ESLint passes without errors  
- [ ] Code is properly formatted
- [ ] Tests pass
- [ ] Build succeeds
- [ ] Documentation updated
```

### Pre-deployment Checklist Template
```markdown
- [ ] All tests pass ✅
- [ ] No TypeScript errors
- [ ] No linting errors
- [ ] Code formatted correctly
- [ ] Build succeeds locally
- [ ] Security audit clean
```

### Post-implementation Checklist Template  
```markdown
- [ ] Feature implemented according to specs
- [ ] Tests written and passing
- [ ] Documentation updated
- [ ] Performance impact validated
- [ ] Accessibility verified
- [ ] Code reviewed
```

## Error Handling Patterns

### Validation with Fallbacks
```bash
# Command with fallback message
pnpm test || echo "No tests found"
pnpm typecheck || echo "TypeScript errors found"
```

### Conditional Execution
```bash
# Only run if file exists
if [ -f "package.json" ]; then
  pnpm install
fi

# Only run if command succeeds
pnpm build && pnpm preview
```

## Documentation Templates

### Workflow Section Headers
```markdown
## Étapes détaillées

### 1. [Action Name]
```bash
# Command description
command-here
```

**Checklist:**
- [ ] Step 1 completed
- [ ] Step 2 verified
```

### Success Metrics Template
```markdown
## Métriques de succès
- **[Metric 1]:** [Target value]
- **[Metric 2]:** [Target value]
- **[Metric 3]:** [Target value]
```

### Output Template
```markdown
## Sortie attendue
- **[Deliverable 1]** avec [criteria]
- **[Deliverable 2]** respectant [standards]
- **[Deliverable 3]** optimisé pour [goals]
```

## Command Patterns

### Git Operations
```bash
# Standard commit workflow
git add .
git commit -m "type(scope): description

- Detail 1
- Detail 2

🤖 Generated with Claude Code

Co-Authored-By: Claude <noreply@anthropic.com>"
```

### File Operations
```bash
# Safe file operations
mkdir -p path/to/directory
touch path/to/file.ext || echo "File creation failed"
```

### Validation Sequences
```bash
# Standard validation flow
echo "=== [STAGE NAME] ==="
command1 && echo "✅ Step 1" || echo "❌ Step 1"
command2 && echo "✅ Step 2" || echo "❌ Step 2"
```

## Reporting Templates

### Progress Report
```bash
echo "=== PROGRESS REPORT ==="
echo "Completed: X/Y tasks"
echo "Status: [In Progress/Completed/Blocked]"
echo "Next: [Next action]"
```

### Error Report  
```bash
echo "=== ERROR ANALYSIS ==="
echo "Issue: [Description]"
echo "Location: [File:Line]"
echo "Fix: [Solution]"
```

These patterns should be referenced in workflows to maintain consistency and reduce duplication across all process documentation.